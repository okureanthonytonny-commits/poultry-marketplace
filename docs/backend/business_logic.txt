User types = [
    'admin',
    'seller',
    'buyer',
]

User_data = [
    id: primary_key,
    name: str,
    user_name: str,
    role: str,
    email: str,
    phone_number: str,
    created_at: datetime,
    updated_at: datetime,
]

admin_data = [
    id: primary_key,
    user_id: foreign_key(User_data.id),
    role: 'admin',
]

seller_data = [
    id: primary_key,
    user_id: foreign_key(User_data.id),
    role: 'seller'
    business_name: str,
    activity_score: float,
]

buyer_data = [
    id: primary_key,
    user_id: foreign_key(User_data.id),
    role: 'buyer',
]

batch_data = [
    id: primary_key,
    batch_number: str,
    supplier_name: str,
    unit_cost: float,
    product_id: foreign_key(product_data.id),
    initial_quantity: int,
    current_quantity: int,
    description: JSONfield,
    manufacture_date: datetime,
    expiry_date: datetime,
    date_recieved: datetime,
    created_at: datetime,
    updated_at: datetime,
]

product_data = [
    id: primary_key,
    seller_id: foreign_key(seller_data.id),
    name: str,
    unit_retail_price: float,
    description: JSONfield,
]

stock_data = [
    id: primary_key,
    product_id: foreign_key(product_data.id),
    quantity: int, # sum of batch_data.current_quantity
    updated_at: datetime, #or we could use  in batch_data table: select(updated_at).where( product_id == batch_data.product_id ).first() 
]

admin_access = [] #the data admin needs to see